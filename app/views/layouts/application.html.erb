<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title yield(:title) %></title>
    <%= render "head_tags" %>
  </head>

  <body>
    <div class="container">
      <%= render "header" if controller_name == "posts" &&
            action_name == "index" %>

      <div class="row">
        <%= render "flash_alerts" %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
